要一步步实现一个 Qt6 图片查看器，最好的方式就是从 最小可用产品（MVP） 开始，每轮迭代叠加一个清晰的功能模块。下面按任务拆解，从最基本的“显示一张图片”开始，逐步加入浏览、交互、缩略图、性能优化等能力，同时列出每个阶段用到的 Qt 核心组件。

---

阶段 1：MVP – 显示单张图片

目标：窗口居中显示一张图片，图片随窗口缩放，保持比例。

用到 Qt 组件：

· QApplication：应用程序入口
· QMainWindow：主窗口框架
· QGraphicsView：视图容器，负责滚动、缩放变换
· QGraphicsScene：场景，管理图元
· QGraphicsPixmapItem：图片图元，直接持有 QPixmap
· QImageReader：读取图片（自动处理 EXIF 旋转）
· QPixmap：显示用像素图

关键实现：

· 在 QMainWindow 中央放置 QGraphicsView，去掉默认滚动条装饰。
· 创建场景并添加 QGraphicsPixmapItem。
· QGraphicsView 设置 fitInView(sceneRect, Qt::KeepAspectRatio) 适应窗口。
· 重写 resizeEvent 保持自适应效果。

---

阶段 2：打开图片文件

目标：通过菜单或按钮选择并打开任意图片文件。

新增 Qt 组件：

· QMenuBar、QMenu：菜单栏
· QAction：菜单项/工具栏按钮
· QFileDialog：系统文件选择对话框

关键实现：

· 添加“文件 → 打开”动作，调用 QFileDialog::getOpenFileName 获取路径。
· 加载图片流程与阶段 1 相同，但改为动态替换场景中的 QGraphicsPixmapItem 像素图。

---

阶段 3：文件夹浏览与上下张导航

目标：打开图片后，能用键盘方向键（← →）浏览同文件夹的所有图片。

新增 Qt 组件：

· QDir、QStringList：列出文件夹内图片文件
· QKeyEvent（重写视图或主窗口事件）：响应键盘
· QStatusBar：状态栏显示“第 3/20 张”
· QPushButton / QToolButton（可选）：上一张/下一张按钮

关键实现：

· 打开图片时，用 QDir 过滤常见图片后缀（*.jpg *.png *.bmp *.webp 等），生成有序文件列表，记录当前索引。
· 切换图片时更新显示并刷新状态栏。

---

阶段 4：缩放与平移交互

目标：鼠标滚轮缩放（以鼠标位置为中心），鼠标拖拽平移，增加“适应窗口/实际大小”按钮。

新增/深入使用的 Qt 组件：

· QGraphicsView::scale()：缩放因子
· QWheelEvent：滚轮事件
· QGraphicsView::setDragMode(QGraphicsView::ScrollHandDrag)：拖拽平移模式
· QGraphicsView::setTransformationAnchor(QGraphicsView::AnchorUnderMouse)：以鼠标为中心缩放
· QToolBar：放置缩放/适应按钮

关键实现：

· 重写 QGraphicsView::wheelEvent，计算缩放因子 1.15/0.85 并调用 scale()。
· “适应窗口”调用 fitInView，“实际大小”重置变换矩阵。
· 工具栏加入切换按钮，连接对应槽函数。

---

阶段 5：缩略图面板

目标：侧边栏显示当前文件夹所有图片的缩略图，点击可快速跳转。

新增 Qt 组件：

· QDockWidget：可停靠面板
· QListWidget：图标模式列表（setViewMode(QListView::IconMode)）
· QListWidgetItem：每个缩略图项
· QIcon：包装缩略图 QPixmap
· QtConcurrent::run / QThread：后台生成缩略图（简单版可主线程生成）

关键实现：

· 浏览文件夹时，为每个文件创建 QListWidgetItem，异步生成缩略图（缩小到固定尺寸如 128x128）。
· 点击缩略图发出信号，切换到对应索引的大图。
· 当前图片高亮选中项，滚动列表确保可见。

---

阶段 6：异步加载与内存缓存

目标：加载大图时不阻塞界面，限制内存占用，预加载前后几张图片。

新增 Qt 组件：

· QtConcurrent::run：将图片读取+缩放放到线程池
· QFutureWatcher：监控异步任务完成
· QCache<QString, QPixmap>：内存缓存，按路径作为键
· QPixmapCache（可选）：全局像素图缓存

关键实现：

· 发起异步任务：QImageReader 设置缩放尺寸（限制为屏幕 2 倍大小），在线程中生成 QPixmap 后通过信号返回。
· 维护一个 QCache，缓存当前图 + 前后各 2~3 张，淘汰远端的图。
· 切换图片时先显示缓存/低清图，再请求高清图更新。

---

阶段 7：全屏模式

目标：按 F11 或按钮进入全屏，隐藏所有装饰，黑色背景沉浸看图。

新增 Qt 组件：

· QMainWindow::showFullScreen() / showNormal()
· QScreen（Qt6 替换 QDesktopWidget）：多屏时管理窗口位置
· 快捷键 QShortcut 或菜单动作

关键实现：

· 全屏时隐藏菜单栏、工具栏、状态栏、缩略图面板，将视图背景设为黑色。
· 退出时恢复界面状态。
· 可在全屏下叠加半透明工具栏（鼠标移动到顶部显示）。

---

阶段 8：旋转与幻灯片播放

目标：旋转当前图片（90°/180°），自动播放下一张（带渐变过渡）。

新增 Qt 组件：

· QGraphicsPixmapItem::setRotation()：旋转图元
· QTimer：定时触发切换
· QPropertyAnimation + QGraphicsEffect（如 QGraphicsOpacityEffect）：淡入淡出过渡

关键实现：

· 旋转：直接对 pixmapItem 设置旋转角度，无需重新生成 QPixmap。
· 幻灯片：QTimer 间隔调用“下一张”，动画可选在切换时使用 QPropertyAnimation 改变图元透明度。

---

阶段 9：拖放支持与扩展（选加）

目标：支持从文件管理器拖入图片或文件夹，直接打开浏览。

新增 Qt 组件：

· 重写 dragEnterEvent、dropEvent
· QMimeData：获取拖入的 URL
· QUrl、QFileInfo：解析本地路径

关键实现：

· 主窗口或视图设置为接受拖放，从 QMimeData::urls() 取出文件路径，打开第一张图片并加载文件夹列表。
· 拖入文件夹则直接浏览该文件夹。

---

通过上面 9 个阶段，你就能从零构建出一个功能完整、体验流畅的 Qt6 图片查看器。每一轮迭代都足够小，可以立刻运行验证，而且用到的 Qt 组件都是标准库自带，无需第三方依赖。
